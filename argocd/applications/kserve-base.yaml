apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kserve-base
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: default
  source:
    repoURL: 'https://github.com/milinddethe15/llm-inference-platform.git'
    path: platform/kserve-base
    targetRevision: main
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: kserve
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
